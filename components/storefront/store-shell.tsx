import Link from "next/link";
import { Search, ShoppingBag } from "lucide-react";
import { themeVars } from "@/lib/storefront";
function storeHref(basePath:string,path=""){return basePath ? `${basePath}${path}` : (path || "/");}
export function StoreShell({store,basePath,children}:{store:Record<string,unknown>;basePath:string;children:React.ReactNode}){const settings=(Array.isArray(store.store_settings)?store.store_settings[0]:store.store_settings) as {header_links?:{label:string;href:string}[];footer_links?:{label:string;href:string}[]} | null;return <div style={themeVars(store)} className="min-h-screen bg-[var(--store-bg)]" ><header className="border-b border-white/10"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-5 py-5"><Link href={storeHref(basePath)} className="text-lg font-semibold tracking-tight">{String(store.name)}</Link><nav className="hidden gap-5 text-sm opacity-70 md:flex">{(settings?.header_links??[]).map((l,i)=><Link key={i} href={l.href}>{l.label}</Link>)}</nav><div className="flex items-center gap-2"><Link href={`${storeHref(basePath)}?search=1`} aria-label="Search" className="rounded-lg border border-white/10 p-2"><Search size={17}/></Link><Link href={storeHref(basePath,"/cart")} aria-label="Cart" className="rounded-lg border border-white/10 p-2"><ShoppingBag size={17}/></Link></div></div></header><main>{children}</main><footer className="mt-20 border-t border-white/10"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-10 text-sm opacity-60 md:flex-row md:justify-between"><span>{String(store.name)}</span><div className="flex flex-wrap gap-4">{(settings?.footer_links??[]).map((l,i)=><Link key={i} href={l.href}>{l.label}</Link>)}</div></div></footer></div>}
