import { notFound } from "next/navigation";
import { getDashboardContext } from "@/lib/dashboard-context";
import { ProductForm } from "@/components/dashboard/product-form";
import { addVariantAction, assignProductCategoryAction, configureCustomDeliveryAction, configureGeneratedProductAction, deleteProductAction, duplicateProductAction, importAccountsAction, importLicensesAction, uploadDigitalFileAction, uploadProductImageAction } from "@/lib/product-actions";
export default async function ProductEdit({params}:{params:Promise<{id:string}>}){const {id}=await params;const ctx=await getDashboardContext(); const {data:p}=await ctx.db.from("products").select("*").eq("id",id).eq("store_id",ctx.store?.id??"").maybeSingle();if(!p)notFound();const [{data:variants},{data:images},{count:licenses},{count:accounts},{data:files},{data:categories},{data:assigned},{data:generated},{data:customDelivery}]=await Promise.all([ctx.db.from("product_variants").select("*").eq("product_id",id),ctx.db.from("product_images").select("*").eq("product_id",id),ctx.db.from("licenses").select("id,license_pools!inner(product_id)",{count:"exact",head:true}).eq("license_pools.product_id",id),ctx.db.from("account_inventory").select("id,account_pools!inner(product_id)",{count:"exact",head:true}).eq("account_pools.product_id",id),ctx.db.from("digital_files").select("id,original_name,size_bytes").eq("product_id",id),ctx.db.from("categories").select("id,name").eq("store_id",p.store_id).order("name"),ctx.db.from("product_categories").select("category_id").eq("product_id",id),ctx.db.from("generated_products").select("endpoint_url").eq("product_id",id).maybeSingle(),ctx.db.from("custom_delivery").select("instructions").eq("product_id",id).maybeSingle()]);return <><div className="mb-6 flex items-center justify-between"><div><h1 className="text-2xl font-semibold">{p.name}</h1><p className="mt-1 text-sm text-sc-secondary">Edit product and fulfillment inventory.</p></div><div className="flex gap-2"><form action={duplicateProductAction}><input type="hidden" name="productId" value={id}/><button className="rounded-lg border border-sc-border px-3 py-2 text-sm">Duplicate</button></form><form action={deleteProductAction}><input type="hidden" name="productId" value={id}/><button className="rounded-lg border border-red-950 bg-red-950/20 px-3 py-2 text-sm text-red-300">Delete</button></form></div></div><ProductForm storeId={p.store_id} product={p}/><div className="mt-6 grid gap-5 xl:grid-cols-2"><section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Variants</h2><div className="mt-3 space-y-2">{variants?.map(v=><div key={v.id} className="grid grid-cols-3 rounded-lg border border-sc-border px-3 py-2 text-sm"><span>{v.name}</span><span className="text-sc-secondary">{v.sku||"No SKU"}</span><span className="text-right text-sc-secondary">{v.inventory_quantity??"∞"}</span></div>)}</div><form action={addVariantAction} className="mt-4 grid grid-cols-2 gap-2"><input type="hidden" name="productId" value={id}/><input name="name" required placeholder="Variant name"/><input name="sku" placeholder="SKU"/><input name="price" type="number" step="0.01" placeholder="Override price"/><input name="inventoryQuantity" type="number" min="0" placeholder="Inventory"/><button className="col-span-2 rounded-lg border border-sc-border px-3 py-2 text-sm">Add variant</button></form></section><section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Images</h2><p className="mt-2 text-sm text-sc-secondary">{images?.length??0} image(s)</p><form action={uploadProductImageAction} className="mt-4 flex gap-2"><input type="hidden" name="productId" value={id}/><input type="file" name="file" accept="image/*" required/><button className="rounded-lg border border-sc-border px-3 text-sm">Upload</button></form></section>{p.type==="license"&&<section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">License inventory</h2><p className="mt-2 text-sm text-sc-secondary">{licenses??0} keys stored. Duplicate keys are ignored by hash.</p><form action={importLicensesAction} className="mt-4 space-y-2"><input type="hidden" name="productId" value={id}/><textarea name="keys" rows={6} placeholder="One license key per line" required/><button className="rounded-lg bg-sc-red px-3 py-2 text-sm">Import keys</button></form></section>}{p.type==="account"&&<section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Account inventory</h2><p className="mt-2 text-sm text-sc-secondary">{accounts??0} encrypted account(s). Use username:password or one JSON object per line.</p><form action={importAccountsAction} className="mt-4 space-y-2"><input type="hidden" name="productId" value={id}/><textarea name="accounts" rows={6} placeholder="user@example.com:secret" required/><button className="rounded-lg bg-sc-red px-3 py-2 text-sm">Import encrypted inventory</button></form></section>}{p.type==="digital_file"&&<section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Private files</h2><div className="mt-2 text-sm text-sc-secondary">{files?.map(f=><div key={f.id}>{f.original_name} · {Math.ceil(f.size_bytes/1024)} KB</div>)}</div><form action={uploadDigitalFileAction} className="mt-4 space-y-2"><input type="hidden" name="productId" value={id}/><input type="file" name="file" required/><input type="number" name="maxDownloads" min="1" placeholder="Max downloads per purchase (optional)"/><button className="rounded-lg bg-sc-red px-3 py-2 text-sm">Upload private file</button></form></section>}{p.type==="generated"&&<section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Generation endpoint</h2><p className="mt-2 text-sm text-sc-secondary">SellCore signs the order-item request with your shared secret after verified payment.</p><form action={configureGeneratedProductAction} className="mt-4 space-y-2"><input type="hidden" name="productId" value={id}/><input type="url" name="endpointUrl" defaultValue={generated?.endpoint_url??""} required placeholder="https://api.example.com/generate"/><input type="password" name="secret" placeholder={generated?"Leave blank to keep current secret":"Signing secret"}/><button className="rounded-lg bg-sc-red px-3 py-2 text-sm">Save generator</button></form></section>}{p.type==="custom"&&<section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Custom fulfillment</h2><form action={configureCustomDeliveryAction} className="mt-4 space-y-2"><input type="hidden" name="productId" value={id}/><textarea rows={5} name="instructions" defaultValue={customDelivery?.instructions??""} placeholder="Instructions delivered after payment"/><button className="rounded-lg bg-sc-red px-3 py-2 text-sm">Save instructions</button></form></section>}<section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Categories</h2><div className="mt-2 text-xs text-sc-secondary">Assigned: {(assigned??[]).map(a=>categories?.find(c=>c.id===a.category_id)?.name).filter(Boolean).join(", ")||"None"}</div>{categories?.length?<form action={assignProductCategoryAction} className="mt-4 flex gap-2"><input type="hidden" name="productId" value={id}/><select name="categoryId">{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="rounded-lg border border-sc-border px-3 py-2 text-sm">Assign</button></form>:<p className="mt-3 text-sm text-sc-secondary">Create a category from Products → Categories.</p>}</section></div></>}
