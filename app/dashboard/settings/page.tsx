import { getDashboardContext } from "@/lib/dashboard-context";
import { updateStoreAction, addTeamMemberAction } from "@/lib/merchant-actions";
import { EmptyState } from "@/components/ui/empty-state";
export default async function Settings(){const ctx=await getDashboardContext();if(!ctx.store)return <EmptyState title="No store"/>;const [{data:s},{data:members}]=await Promise.all([ctx.db.from("store_settings").select("*").eq("store_id",ctx.store.id).single(),ctx.db.from("merchant_members").select("role,user_id,users(email,display_name)").eq("merchant_id",ctx.merchantId)]);return <><h1 className="text-2xl font-semibold">Settings</h1><p className="mt-1 text-sm text-sc-secondary">General store configuration, customer behavior and team access.</p><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_420px]"><form action={updateStoreAction} className="rounded-xl border border-sc-border bg-sc-card p-5"><input type="hidden" name="storeId" value={ctx.store.id}/><h2 className="font-semibold">General</h2><div className="mt-4 grid gap-4"><label className="text-sm">Store name<input name="name" defaultValue={ctx.store.name} required/></label><label className="text-sm">Description<textarea name="description" rows={4} defaultValue={ctx.store.description??""}/></label><label className="text-sm">Currency<input name="currency" maxLength={3} defaultValue={ctx.store.default_currency||"USD"}/></label><label className="text-sm">SEO title<input name="title" defaultValue={s?.title??ctx.store.name}/></label><label className="text-sm">Meta description<textarea name="metaDescription" rows={3} defaultValue={s?.meta_description??""}/></label><label className="text-sm">Support email<input name="supportEmail" type="email" defaultValue={s?.support_email??""}/></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" className="h-4 w-4" name="verifiedReviewsOnly" defaultChecked={s?.verified_reviews_only??true}/> Require verified purchase for public reviews</label><button className="rounded-lg bg-sc-red px-4 py-2 text-sm">Save settings</button></div></form><section className="rounded-xl border border-sc-border bg-sc-card p-5"><h2 className="font-semibold">Team</h2><div className="mt-4 space-y-2">{members?.map(m=><div key={m.user_id} className="rounded-lg border border-sc-border p-3 text-sm"><div>{(m.users as {display_name?:string}|null)?.display_name||(m.users as {email?:string}|null)?.email}</div><div className="text-xs text-sc-muted">{m.role}</div></div>)}</div><form action={addTeamMemberAction} className="mt-4 space-y-2"><input type="email" name="email" required placeholder="Existing SellCore user email"/><select name="role"><option value="staff">Staff</option><option value="admin">Admin</option><option value="viewer">Viewer</option></select><button className="w-full rounded-lg border border-sc-border px-3 py-2 text-sm">Add member</button></form></section></div></>}
