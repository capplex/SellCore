import { getDashboardContext } from "@/lib/dashboard-context";
import { createCouponAction, toggleCouponAction } from "@/lib/merchant-actions";
import { EmptyState } from "@/components/ui/empty-state";
export default async function Coupons(){const ctx=await getDashboardContext();if(!ctx.store)return <EmptyState title="No store"/>;const {data}=await ctx.db.from("coupons").select("*").eq("store_id",ctx.store.id).order("created_at",{ascending:false});return <><h1 className="text-2xl font-semibold">Coupons</h1><p className="mt-1 text-sm text-sc-secondary">Discounts are validated against server-side product prices at checkout.</p><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_420px]"><div className="space-y-3">{!data?.length?<EmptyState title="No coupons"/>:data.map(c=><div key={c.id} className="flex items-center justify-between rounded-xl border border-sc-border bg-sc-card p-4"><div><div className="font-medium">{c.code}</div><div className="text-xs text-sc-muted">{c.discount_type} · {c.discount_type==="percent"?`${c.discount_value/100}%`:`${c.discount_value/100} ${c.currency}`}</div></div><form action={toggleCouponAction}><input type="hidden" name="couponId" value={c.id}/><button className="rounded-lg border border-sc-border px-3 py-2 text-sm">{c.active?"Disable":"Enable"}</button></form></div>)}</div><form action={createCouponAction} className="rounded-xl border border-sc-border bg-sc-card p-5"><input type="hidden" name="storeId" value={ctx.store.id}/><h2 className="font-semibold">Create coupon</h2><div className="mt-4 grid gap-3"><input name="code" required placeholder="WELCOME10"/><select name="discountType"><option value="percent">Percentage</option><option value="fixed">Fixed amount</option></select><input name="discountValue" type="number" min="0.01" step="0.01" required placeholder="10"/><input name="currency" defaultValue={ctx.store.default_currency||"USD"}/><input name="minimumOrder" type="number" min="0" step="0.01" placeholder="Minimum order"/><input name="usageLimit" type="number" min="1" placeholder="Total usage limit"/><input name="perCustomerLimit" type="number" min="1" placeholder="Per-customer limit"/><input name="expiresAt" type="datetime-local"/><button className="rounded-lg bg-sc-red px-4 py-2 text-sm">Create coupon</button></div></form></div></>}
