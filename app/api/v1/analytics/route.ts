import { authenticateApiRequest,apiError,assertApiStore } from "@/lib/api/auth";
export async function GET(req:Request){try{const {db,merchantId}=await authenticateApiRequest(req,"analytics:read");const url=new URL(req.url);const storeId=url.searchParams.get("store_id");if(!storeId)throw Object.assign(new Error("store_id is required"),{status:400});await assertApiStore(db,merchantId,storeId);const from=url.searchParams.get("from")??new Date(Date.now()-30*86400000).toISOString();const {data,error}=await db.from("analytics_events").select("id,event_type,product_id,order_id,source,created_at,metadata").eq("store_id",storeId).gte("created_at",from).order("created_at",{ascending:false}).limit(5000);if(error)throw error;return Response.json({data});}catch(e){return apiError(e)}}
