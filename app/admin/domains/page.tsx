import { createSupabaseAdminClient } from "@/lib/supabase/admin";
export default async function Domains(){const db=createSupabaseAdminClient();const {data}=await db.from("domains").select("id,hostname,status,verified_at,created_at,stores(name)").order("created_at",{ascending:false});return <><h1 className="text-2xl font-semibold">Domains</h1><div className="mt-5 space-y-2">{data?.map(d=><div key={d.id} className="grid grid-cols-3 rounded-lg border border-sc-border bg-sc-card p-3 text-sm"><span>{d.hostname}</span><span>{(d.stores as {name?:string}|null)?.name}</span><span className="text-right text-sc-secondary">{d.status}</span></div>)}</div></>}
