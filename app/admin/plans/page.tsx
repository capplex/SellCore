import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { createPlanAction,updatePlanAction } from "@/lib/admin-actions";
export default async function Plans(){const db=createSupabaseAdminClient();const {data:plans}=await db.from("platform_plans").select("*,plan_limits(*),plan_features(*)").order("sort_order");return <><h1 className="text-2xl font-semibold">Platform plans</h1><p className="mt-1 text-sm text-sc-secondary">This configuration powers pricing, billing display and server-side entitlements.</p><div className="mt-5 space-y-5">{plans?.map(p=><form action={updatePlanAction} key={p.id} className="rounded-xl border border-sc-border bg-sc-card p-5"><input type="hidden" name="planId" value={p.id}/><div className="grid gap-3 md:grid-cols-4"><input name="name" defaultValue={p.name}/><input name="currency" defaultValue={p.currency}/><input name="monthlyPrice" type="number" step="0.01" defaultValue={p.monthly_price_minor/100}/><input name="yearlyPrice" type="number" step="0.01" defaultValue={p.yearly_price_minor/100}/><input name="trialDays" type="number" min="0" max="365" defaultValue={p.trial_days||0} placeholder="Trial days"/></div><textarea className="mt-3" name="description" defaultValue={p.description??""}/><div className="mt-3 grid gap-2 md:grid-cols-4">{p.plan_limits.map((l:{limit_key:string;limit_value:number|null})=><label key={l.limit_key} className="text-xs text-sc-secondary">{l.limit_key}<input name={`limit:${l.limit_key}`} type="number" defaultValue={l.limit_value??""}/></label>)}</div><div className="mt-4 flex flex-wrap gap-3">{p.plan_features.map((f:{feature_key:string;enabled:boolean})=><label key={f.feature_key} className="flex items-center gap-2 text-xs text-sc-secondary"><input className="h-4 w-4" type="checkbox" name={`feature:${f.feature_key}`} defaultChecked={f.enabled}/>{f.feature_key}</label>)}</div><div className="mt-4 grid gap-3 md:grid-cols-2"><input name="stripeMonthlyPriceId" placeholder="Stripe monthly Price ID" defaultValue={p.stripe_monthly_price_id??""}/><input name="stripeYearlyPriceId" placeholder="Stripe yearly Price ID" defaultValue={p.stripe_yearly_price_id??""}/></div><label className="mt-4 flex items-center gap-2 text-sm"><input className="h-4 w-4" type="checkbox" name="isActive" defaultChecked={p.is_active}/> Active</label><button className="mt-4 rounded-lg bg-sc-red px-4 py-2 text-sm">Save plan</button></form>)}</div><form action={createPlanAction} className="mt-6 rounded-xl border border-dashed border-sc-border p-5"><h2 className="font-semibold">Create paid plan</h2><div className="mt-4 grid gap-3 md:grid-cols-3"><input name="name" placeholder="Name" required/><input name="slug" pattern="[a-z0-9-]+" placeholder="slug" required/><input name="currency" defaultValue="USD"/><input name="monthlyPrice" type="number" step="0.01" placeholder="Monthly price"/><input name="yearlyPrice" type="number" step="0.01" placeholder="Yearly price"/><input name="trialDays" type="number" min="0" max="365" defaultValue="0" placeholder="Trial days"/><input name="sortOrder" type="number" defaultValue="50"/></div><textarea name="description" className="mt-3" placeholder="Description"/><button className="mt-3 rounded-lg border border-sc-border px-4 py-2 text-sm">Create plan</button></form></>}
