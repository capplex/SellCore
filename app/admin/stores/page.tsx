import { createSupabaseAdminClient } from "@/lib/supabase/admin";
export default async function Stores(){const db=createSupabaseAdminClient();const {data}=await db.from("stores").select("id,name,slug,status,created_at,merchants(name)").order("created_at",{ascending:false}).limit(300);return <><h1 className="text-2xl font-semibold">Stores</h1><div className="mt-5 overflow-x-auto rounded-xl border border-sc-border"><table className="w-full min-w-[650px] text-sm"><thead><tr className="border-b border-sc-border text-left text-xs text-sc-muted"><th className="p-3">Store</th><th>Merchant</th><th>Status</th><th>Created</th></tr></thead><tbody>{data?.map(s=><tr key={s.id} className="border-b border-sc-border"><td className="p-3">{s.name}<div className="text-xs text-sc-muted">{s.slug}</div></td><td>{(s.merchants as {name?:string}|null)?.name}</td><td>{s.status}</td><td>{new Date(s.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></>}
