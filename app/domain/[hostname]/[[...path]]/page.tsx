import { notFound, redirect } from "next/navigation";
import Image from "next/image";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { getPublicProduct,getPublicStoreByHostname,getStoreProducts,publicImageUrl } from "@/lib/storefront";
import { StoreShell } from "@/components/storefront/store-shell";
import { ProductCard } from "@/components/storefront/product-card";
import { TrackView } from "@/components/storefront/track-view";
import { AddToCart } from "@/components/storefront/add-to-cart";
import { CartClient } from "@/components/storefront/cart-client";
import { formatMoney } from "@/lib/utils";
export const dynamic="force-dynamic";
export default async function CustomDomain({params,searchParams}:{params:Promise<{hostname:string;path?:string[]}>;searchParams:Promise<{q?:string}>}){const {hostname,path=[]}=await params;const q=await searchParams;const store=await getPublicStoreByHostname(decodeURIComponent(hostname));const basePath="";if(path.length===0){const products=await getStoreProducts(store.id,q.q);return <StoreShell store={store} basePath={basePath}><TrackView storeSlug={store.slug}/><section className="mx-auto max-w-7xl px-5 py-20"><p className="text-sm font-medium text-[var(--accent)]">DIGITAL STOREFRONT</p><h1 className="mt-3 text-5xl font-semibold tracking-tight">{store.name}</h1>{store.description&&<p className="mt-4 max-w-2xl opacity-60">{store.description}</p>}<form className="mt-8 max-w-xl"><input name="q" defaultValue={q.q} placeholder="Search products" className="border-white/10 bg-white/[.035]"/></form></section><section className="mx-auto grid max-w-7xl gap-4 px-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{products.map(p=><ProductCard key={p.id} product={p} basePath={basePath}/>)}</section></StoreShell>}if(path[0]==="cart"){return <StoreShell store={store} basePath={basePath}><section className="mx-auto max-w-6xl px-5 py-12"><h1 className="mb-6 text-3xl font-semibold">Cart</h1><CartClient storeSlug={store.slug}/></section></StoreShell>}if(path[0]==="product"&&path[1]){const p=await getPublicProduct(store.id,path[1]);const first=p.product_images?.sort((a:{sort_order:number},b:{sort_order:number})=>a.sort_order-b.sort_order)[0];return <StoreShell store={store} basePath={basePath}><TrackView storeSlug={store.slug} productId={p.id}/><section className="mx-auto grid max-w-7xl gap-10 px-5 py-12 lg:grid-cols-2"><div className="overflow-hidden rounded-[var(--radius)] border border-white/10 bg-white/[.025]">{first?<Image src={publicImageUrl(first.storage_path)} alt={first.alt_text||p.name} width={1200} height={900} className="h-full w-full object-cover"/>:<div className="aspect-[4/3]"/>}</div><div className="lg:py-6"><div className="text-xs uppercase opacity-50">{p.type.replaceAll("_"," ")}</div><h1 className="mt-3 text-4xl font-semibold">{p.name}</h1><div className="mt-4 text-xl font-semibold">{formatMoney(p.price_minor,p.currency)}</div><p className="mt-6 whitespace-pre-wrap leading-7 opacity-65">{p.description}</p><div className="mt-8 max-w-md"><AddToCart basePath="" storeSlug={store.slug} productId={p.id} variants={(p.product_variants??[]).filter((v:{active:boolean})=>v.active).map((v:{id:string;name:string})=>({id:v.id,name:v.name}))}/></div></div></section><section className="mx-auto max-w-4xl px-5 py-12"><h2 className="text-2xl font-semibold">Reviews</h2><div className="mt-5 space-y-3">{p.reviews.map((r:{id:string;rating:number;review_text:string;verified_purchase:boolean})=><article key={r.id} className="rounded-[var(--radius)] border border-white/10 p-5"><div className="text-sm font-medium">{r.rating}/5 {r.verified_purchase&&<span className="ml-2 text-[var(--accent)]">Verified purchase</span>}</div><p className="mt-3 opacity-70">{r.review_text}</p></article>)}</div></section></StoreShell>}if(path[0]==="page"&&path[1]){const db=createSupabaseAdminClient();const {data:p}=await db.from("pages").select("*").eq("store_id",store.id).eq("slug",path[1]).eq("status","active").maybeSingle();if(!p)notFound();const blocks=(p.content??[]) as {text?:string}[];return <StoreShell store={store} basePath={basePath}><article className="mx-auto max-w-3xl px-5 py-16"><h1 className="text-4xl font-semibold">{p.title}</h1>{blocks.map((b,i)=><p key={i} className="mt-6 whitespace-pre-wrap leading-7 opacity-70">{b.text}</p>)}</article></StoreShell>}if(path[0]==="account")redirect(`${process.env.NEXT_PUBLIC_APP_URL}/account`);notFound();}
